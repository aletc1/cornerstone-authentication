import 'reflect-metadata';
import { expect } from 'chai';
import communicator from '../../src/index'
import 'mocha';

describe('Types', () => {
    it('TODO test', (done) => {
        debugger;
        done();
    });
});